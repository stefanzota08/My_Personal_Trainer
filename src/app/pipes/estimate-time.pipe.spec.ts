import { EstimateTimePipe } from './estimate-time.pipe';

describe('EstimateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new EstimateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
